# Scout web frontend config. Copy to .env.local for local dev.
# Point at the running Scout backend; if unreachable, the UI falls back to demo data.
VITE_API_URL=http://localhost:8000
VITE_STORE_ID=cortexium-sim
